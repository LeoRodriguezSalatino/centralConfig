server:
  port: 8080
  cloud:
    gateway:
      routes:
        - id: library #identificador de la ruta
          uri: http://localhost:8081 #url del redirect
          predicates: #reglas de analisis del request
            - Path=/library/** #path de url de request a considerar